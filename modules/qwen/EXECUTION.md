# 通义千问代码执行约定

通义千问需要计算或使用扩展能力时，一条消息只能输出一个 JavaScript 代码块。代码块后的文本（可以为空）是本次操作的说明，需要授权时会一并展示。

全部工具调用完成后，最终回复可以在末尾附加一个 `suggestions` 围栏代码块，每行写一条用户可能继续发送的简短回复。界面会将其渲染为可点击的发送按钮；它不能与 `sandbox`、`app` 或 `background` 执行代码块出现在同一条中间回复中。

## `sandbox`

```sandbox
[1, 2, 3].map((value) => value * 2)
```

- 在扩展的隔离沙箱中执行，不弹出授权询问。
- 适合纯计算、筛选、聚合和数据格式转换。
- 不能访问 `app.html`、页面 DOM、扩展后台或 `chrome.*` API。
- 单个表达式可直接作为结果返回；多条语句需要显式 `return`。

## `app`

```app
ve.login()
```

- 代码由隔离执行器求值，并通过受控桥访问当前 `app.html`；执行前需要用户授权。
- 可以直接调用 app 页面上的全局函数，无需额外的字符串路径调用器。
- 各课程模块的操作会安装到页面上的同名命名空间中，例如 `ve.login()`、`ve.assignments({...})`、`ykt.courseList()`。
- 模块操作仍会经过统一的操作注册表和参数处理。课程、作业等数据操作不再自动触发登录，应由千问先调用相应平台的 `login()` 并根据返回值决定是否继续。
- 单个函数调用可以直接作为代码块末尾的表达式。扩展会自动等待其 Promise，因此无需书写 `await`；多条语句仍可按普通异步 JavaScript 使用 `await` 和 `return`。
- 支持无 `await` 的结果组合，例如 `ve.assignments_of_({ courseId: ve.courseList().find(item => item.name === "高等数学").id })`。数组回调在沙箱本地执行，最终参数解析完成后才调用外部操作。
- 页面对象同样通过跨上下文桥访问，函数参数及返回值必须可序列化；真实 DOM 节点不能跨上下文传递。

## `background`

```background
chrome.tabs.query({})
```

- 代码由隔离执行器求值，并通过受控桥访问扩展的后台 Service Worker；执行前需要用户授权。
- 可以直接调用后台全局函数或扩展已有权限的 `chrome.*` API，无需额外的字符串路径调用器。
- 常见用途包括查询或管理标签页、读取扩展存储、读取 Cookies、发送系统通知，以及调用后台模块公开的函数。
- 后台没有页面 DOM；不能使用 `document` 或依赖页面元素。
- Service Worker 可能休眠，不应依赖未持久化的长期内存状态。
- 参数和返回值必须能够跨上下文传递。函数、DOM 节点、循环引用和过深对象只会返回受限的可序列化描述。

## 授权与限制

- `sandbox` 自动执行；`app` 和 `background` 可选择允许一次、在当前一轮中始终允许该执行环境、在当前聊天会话中始终允许该执行环境，或在所有会话中始终允许代码中识别出的扩展操作。
- “所有会话始终允许”的操作也可在扩展选项中逐项设置；关闭某项操作本身时，其始终允许设置会同步关闭。
- `qwen.listOperations` 和 `qwen.getDoc` 默认在所有会话中始终允许，且不能在扩展选项中取消。
- 用户授权后不再按操作风险附加拦截；操作执行没有超时时间，会持续等待实际结果。
- 不再提供 `qwen.executeJs` 操作。代码执行由上述末尾代码块直接触发。
