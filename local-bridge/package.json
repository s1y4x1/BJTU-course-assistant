{
  "name": "bjtu-course-assistant-local-bridge",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Local MCP, HTTP and WebSocket bridge for BJTU Course Assistant",
  "scripts": {
    "start": "node src/server.js",
    "show-token": "node src/server.js --show-token",
    "check": "node --check src/server.js && node --check src/config.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "ws": "8.21.3",
    "zod": "^3.25.0"
  }
}
